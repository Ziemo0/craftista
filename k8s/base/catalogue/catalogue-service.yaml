apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: default
spec:
  selector:
    app: catalogue
  type: ClusterIP
  sessionAffinity: None
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
