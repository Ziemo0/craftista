apiVersion: v1
kind: Service
metadata:
  name: catalogue-db
  namespace: default
spec:
  selector:
    app: catalogue-db
  type: ClusterIP
  sessionAffinity: None
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - port: 5432
    protocol: TCP
    targetPort: 5432
