apiVersion: v1
kind: Service
metadata:
  name: recommendation
  namespace: default
spec:
  selector:
    app: recommendation
  type: ClusterIP
  sessionAffinity: None
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
