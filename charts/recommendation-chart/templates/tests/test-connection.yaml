apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "recommendation-chart.fullname" . }}-test-connection"
  labels:
    {{- include "recommendation-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "recommendation-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
