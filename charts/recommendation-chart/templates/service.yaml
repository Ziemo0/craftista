apiVersion: v1
kind: Service
metadata:
  name: {{ include "recommendation-chart.fullname" . }}
  labels:
    {{- include "recommendation-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recommendation-chart.selectorLabels" . | nindent 4 }}
