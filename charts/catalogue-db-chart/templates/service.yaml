apiVersion: v1
kind: Service
metadata:
  name: {{ include "catalogue-db-chart.fullname" . }}
  labels:
    {{- include "catalogue-db-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "catalogue-db-chart.selectorLabels" . | nindent 4 }}
