apiVersion: v1
kind: Service
metadata:
  name: {{ include "voting-chart.fullname" . }}
  labels:
    {{- include "voting-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voting-chart.selectorLabels" . | nindent 4 }}
